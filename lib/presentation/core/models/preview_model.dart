class PreviewModel {
  final double total, rate, amount;
  final bool isFrom;
  PreviewModel({
    required this.amount,
    required this.rate,
    required this.total,
    required this.isFrom,
  });
}
